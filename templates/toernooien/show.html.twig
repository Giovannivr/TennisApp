{% extends 'base.html.twig' %}

{% block title %}Toernooien{% endblock %}

{% block body %}
    <h1>Toernooien</h1>

    <table class="table" style="color: white">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ toernooien.id }}</td>
            </tr>
            <tr>
                <th>Naam</th>
                <td>{{ toernooien.naam }}</td>
            </tr>
            <tr>
                <th>Datum</th>
                <td>{{ toernooien.datum ? toernooien.datum|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('toernooien_index') }}">back to list</a>

    <a href="{{ path('toernooien_edit', {'id': toernooien.id}) }}">edit</a>

    {{ include('toernooien/_delete_form.html.twig') }}
{% endblock %}
